{
  "name": "logging_middleware",
  "version": "1.0.0",
  "description": "Reusable logging middleware that sends structured logs to the evaluation server",
  "main": "index.js",
  "scripts": {
    "test": "node test.js"
  },
  "dependencies": {
    "axios": "^1.7.0"
  }
}
